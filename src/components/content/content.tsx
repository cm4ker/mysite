import React from 'react';
import './content.css';

const Content: React.FC = () => {
  return (
    <section className="hero">
      {/* Hero section - компактный вариант без контента */}
    </section>
  );
};

export default Content;